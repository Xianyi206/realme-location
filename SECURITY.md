# 源码与发布安全

不要将签名私钥、口令文件、环境配置、真实地点记录或设备日志上传到仓库、Issue 或 Release。签名证书的公开指纹可以发布，私钥不能发布。

构建所需 SDK/JDK 路径通过 `ANDROID_HOME`、`JAVA_HOME` 配置。首次生成开发签名使用随机口令，保存在被 Git 排除的 `.keys/development-password.txt`。已有签名通过环境变量 `DD_KEYSTORE_PASSWORD` 提供口令；构建不会替换已有私钥。私钥和口令文件应备份到受控的私人存储。

源码打包仅选取 Git 跟踪文件，拒绝已知敏感路径、符号链接以及部分常见令牌格式。提交和 PR 运行相同检查。该检查是针对本仓库的防误传措施，不是全面的密钥发现工具；无告警不能证明不存在所有秘密或漏洞。

发布前运行：

```text
python -m unittest discover -s tests -p test_source_safety.py
python scripts/source_safety.py
```

仍需人工检查 Git 历史、Release 附件和报告。删除当前文件或添加 `.gitignore` 不会删除历史版本。若曾上传有效令牌，先吊销或轮换；若签名私钥泄露，需要单独评估签名迁移及旧安装包升级影响。历史重写应另行协调，避免误认为一次普通提交已清除历史。

应用使用本地私有存储，禁止云备份；内部编辑页及定位服务不导出，通知操作使用不可变 PendingIntent。地图 WebView 禁止文件和内容访问，只允许打包资源及指定地图来源，不提供原生 JavaScript 桥。浏览器取得的模拟位置及 realme 兼容性仍需实机验证。

报告安全问题时只提供复现步骤和脱敏内容，不附带真实坐标、口令或私钥。仓库权限和可见性保持由所有者管理。
